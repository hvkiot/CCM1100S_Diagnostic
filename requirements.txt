# Core CAN communication (works on Python 3.13)
python-can==4.4.2

# Cryptography for security access
pycryptodome==3.20.0

# Data validation (optional)
pydantic==2.10.6

# DBus for Bluetooth (system package, not pip)
# dbus-python is installed via apt, not pip